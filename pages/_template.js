import React from 'react'

// CSS.
import '../css/main.scss'

// -------------------------------------------------------------
// Component.
// -------------------------------------------------------------

export default (props) => {
  return <div>
    {props.children}
  </div>
}
